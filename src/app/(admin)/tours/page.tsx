import { Map } from "lucide-react";
import { ModulePlaceholder } from "@/components/ui/module-placeholder";

export default function Page() {
  return (
    <ModulePlaceholder
      icon={Map}
      title="Tours"
      description="Plan and track tours, participants, and status per guest."
    />
  );
}
